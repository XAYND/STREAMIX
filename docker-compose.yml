services:
  nginx:
    image: nginx:latest
    container_name: streamix-nginx
    ports:
      - "8080:80"
      - "8443:443"
    volumes:
      - ./media/hls:/usr/share/nginx/html/hls
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./certs:/etc/nginx/certs